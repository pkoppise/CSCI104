#include <iostream>
#include <algorithm>
using namespace std;
/*
// You can prototype a helper function here if you need it
int amaxh(int* array, int len, int idx);

// This is the primary max() function that the user should call
int amax(int* array, int len)
{
	return amaxh(array,len,0);
}

// You can write your helper function here if you need it
int amaxh(int* array, int len, int idx)
{
	//base case
	if(idx == len) { return -1; }
	else
	{
		int currmax = amaxh(array, len, idx + 1);
		return std::max(currmax, array[idx]);
	}
}


int main() {
   int d1[] = {7, 3, 18, 6, 5, 10, 4, 2};
   cout << amax(d1, 8);
   return 0;
}*/
