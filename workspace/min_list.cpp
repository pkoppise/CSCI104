#include <iostream>
#include <algorithm>
using namespace std;
/*
// You can prototype a helper function here if you need it
int aminh(int* array, int len, int idx);

// This is the primary max() function that the user should call
int amin(int* array, int len)
{
	return aminh(array,len,0);
}

// You can write your helper function here if you need it
int aminh(int* array, int len, int idx)
{
	//base case
	if(idx == len) { return 100; }
	else
	{
		int currmin = aminh(array, len, idx + 1);
		return std::min(currmin, array[idx]);
	}
}

int main() {
   int d1[] = {7, 3, 2, 6, 5, 10, 4, 9};
   cout << amin(d1, 8);
   return 0;
}*/
