#include <iostream>
using namespace std;

//Unary operators are the operators that act upon a single operand to produce a new value
/*
int main()
{
	//-, ++, --, !, &, sizeof()---->unary operators
	// -
    int a = 10;
    int b = -a;
    cout << b << endl;

    // ++
    int c = 10;
    int d = c++;
    cout << d << endl;

    int e = 20;
    int f = ++e;
    cout << f << endl;

    // same as above use --
    // !
    bool x = true;
    bool y = !x;
    cout << y << endl;

    //&
    int h = 10;
    int *ptr = &h;
    cout << ptr << endl;

    //sizeof
    float n = 0;
    cout << "size of n: " << sizeof(n) << endl;

    return 0;
}*/

