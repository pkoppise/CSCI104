#include <iostream>
#include <vector>
using namespace std;

/*
int fact(int n)
{
    if (n <= 1) { // base case
        return 1;
    }
    else
    {
        int ans = fact(n-1);
        retuen (n * ans);
    }
}

void printFun(int test)
{
	if (test < 1)
		return;
	else
	{
		cout << test << " " << endl;
		printFun(test-1);
		printFun(test-1);
		cout << test << " " << endl;
		return;
	}
}

int sum_array(vector<int>& mylist, int len)
{
	//base case
	if(len == 1)
	{
		return mylist[0];
	}
	else
	{
		int sum =  sum_array(mylist, len-1 );
        return (sum + mylist[len-1]);
	}
}

void bincombos(string prefix, int len) {
   // base case
   if(int(prefix.size()) == len)
   {
	   cout << prefix << endl;
	   return;
   }
   //recursive case
   else
   {
	   bincombos(prefix + "0", len);
	   bincombos(prefix + "1", len);
   }
}

int main()
{
	//cout << fact(5) << endl;

	//int test = 3;
	//printFun(test);

    //int d1[] = {3,4,5,6,7};
    //vector<int> v1(d1, d1 + 5);
    //cout << sum_array(v1,v1.size()) << endl;

	//int numBits;
	//cout << "Enter the number of bits:";
	//cin >> numBits;
	//bincombos("",numBits);

	return 0;
}*/


