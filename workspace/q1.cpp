#include<iostream>
#include<string>
#include<set>

using namespace std;
/*
int main()
{
	string str = "abccad";
    set<char> temp;

    for(unsigned int i = 0; i < str.size(); i++)
    {
    	temp.insert(str[i]);
    }

    string unq = "";
    set<char>::iterator it;
    for(it = temp.begin(); it != temp.end(); ++it)
    {
    	unq.push_back(*it);
    }
    cout <<  "Rev String:" << unq << endl;
}*/
