// A C program to demonstrate different storage
// classes
#include <iostream>
using namespace std;
/*
// declaring and initializing an extern variable
extern int x = 9;

// declaring and initialing a global variable z
// simply int z; would have initialized z with
// the default value of a global variable which is 0
int z = 10;

int main()
{
    // declaring an auto variable (simply writing "int a=32;" works as well)
    auto a = 32;

    // declaring a register variable
    register char b = 'G';

    // telling the compiler that the variable, z is an extern variable and has been
    // defined elsewhere (above the main function)
    extern int z;

    cout << "Hello World" << endl;;

    // printing the auto variable 'a'
    cout << "This is the value of the auto integer 'a':" << a << endl;

    // printing the extern variables 'x'and 'z'
    cout << "These are the values of the extern integers 'x' and 'z':" << x << " " << z << endl;

    // printing the register variable 'b'
    cout << "This is the value of the register character 'b':" << b << endl;

    x = 2;
    z = 5;
    cout << "These are the values of the extern integers 'x' and 'z' after modifying:" << x << " " << z << endl;

    return 0;
}*/
