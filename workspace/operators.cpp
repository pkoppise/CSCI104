#include <iostream>
using namespace std;

/*
int main()
{
	//Bit-wise operator
	int a = 10, b = 12;
    cout << (a & b) << endl;   //AND
    cout << (a | b) << endl;   // OR
    cout << (a ^ b) << endl;   // XOR
    cout << (b << 1) << endl;  // left shift
    cout << (a >> 1) << endl;  // right shift
    cout << (~b) << endl;      // NOT operator

    //sizeof() operator
    cout << sizeof(char) << endl;
	cout << sizeof(int) << endl;
	cout << sizeof(float) << endl;
	cout << sizeof(double) << endl;

	int x = 0;
	int y = 10.21;
	cout << sizeof(x + y) << endl;

	int *ptr = (int *)malloc(10*sizeof(int));
	free(ptr);

    //comma operator
	int i = (5, 10);
	cout << i << endl; // i value is 10
    return 0;
}*/
