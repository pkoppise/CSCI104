#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
/*
void allCombos(const vector<char>& letters, int n, string data)
{
	if(int(data.size()) == n)
	{
		cout << data << endl;
		return;
	}
	else
	{
		for(int i = 0; i < int(letters.size()); i++)
		{
			allCombos(letters, n, data + letters[i]);
		}
	}
}


int main(int argc, char* argv[])
{
   int n = 2;
   vector<char> options;

   options.push_back('U');
   options.push_back('S');
   options.push_back('C');

   allCombos(options, n, "");
   return 0;
}*/
