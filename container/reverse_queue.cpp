#include <iostream>
#include <queue>
#include <stack>

using namespace std;
/*
void showq(queue <int> gq)
{
	queue <int> g = gq;
	while (!g.empty())
	{
		cout << '\t' << g.front();
		g.pop();
	}
	cout << '\n';
}

void reverse(queue<int>& gquiz)
{
	stack<int> temp;
	while(!gquiz.empty())
	{
		temp.push(gquiz.front());
		gquiz.pop();
	}

	while(!temp.empty())
	{
		gquiz.push(temp.top());
		temp.pop();
	}
}

int main()
{
	queue <int> gquiz;
	gquiz.push(10);
	gquiz.push(20);
	gquiz.push(30);

	cout << "The queue gquiz is : ";
	showq(gquiz);
	reverse(gquiz);
	cout << "The reverse of queue gquiz is : ";
	showq(gquiz);
    return 0;
}*/
