#include <iostream>
#include <iomanip>
using namespace std;
/*
int main()
{
	float c = 5.0;
	float far = (float(9)/5)*c + 32;
	float fat = (9/5)*c + 32;
	cout << fixed << setprecision(2) << far << " " << fat << endl;

	int x = 3, y = 5, z = 6;
	int a = 2, b = 4, c = 7;

	int res1 = c^(x^y); // XOR
	int res2 = z^(a^b);

	cout << res1 << " " << res2 << endl;

	int h = x << a;  //left shift
	int p = h << x;
	cout << (h >> 1) << " " << (p >> 1) << endl; //right shift

	int i = 1024;
	for(;i;i >>= 1) //right shift 1 and assign ; loop goes until i drops to zero i.e. 11 times
	{
		cout << "GeeksQuiz" << endl;
	}

	int j;
	if(printf("0")) //prints 0 and returns number of characters being printed i.e. 1
		j = 3;
	else
		j = 5;
	cout << j << endl;

	int k = 5;
	do{
		cout << k << endl;
	}while(k--);

	cout << "------------" << endl;
	for(int t = 0; t < 5; t++)
	{
		cout << t << endl;
	}

	int tt = 0;
	switch(tt)
	{
	case 0:
		cout << "case 0" << endl;
		break;
	case 1:
		cout << "case 1" << endl;
		break;
	}

    return 0;
}*/
//sizeof(int) > -1 --> 2's compliment greater than size of int.
//Indicators in c++ --->error , EndOfFile, Position
//cin >> i + 4--->addition operation on cin is not allowed.
//groups of output---->formatted and unformatted
//a += 5---->compound assignment
//a = 0 && b = 10---->false
//---> /, *, + , -
//i >>= 1
