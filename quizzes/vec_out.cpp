#include <iostream>
#include <vector>
using namespace std;
/*
int main()
{
	vector<int> vec;
	for(int i = 1; i <= 5; i++)
	{
		vec.push_back(i);
	}

	for(int i = 0; i <5; i++)
	{
		//cout << vec[i] << endl;
	}

	vector<int> a = {2,2,2,5,5,7,7,8,8,9};
	vector<int>::iterator lower,upper,it;

	lower = lower_bound(a.begin(), a.end(), 1);
	//upper = upper_bound(a.begin(), a.end(), 5);
	cout << (lower - a.begin()) << " " << *lower << endl;

	//reverse(a.begin() + 3, a.end() - 1);
	for(it = a.begin(); it != a.end(); it++)
	{
		cout << *it << " ";
	}

}*/

//advance(it,3)
//random_shuffle
//sort(arr + 2, arr + end, greater())
//reverse
