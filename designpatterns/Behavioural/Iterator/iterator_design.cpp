#include <iostream>
#include <vector>

using namespace std;

/*
int main()
{
	vector<string> vec{"Hello", "Good", "Morning"};
	vector<string>::iterator it = vec.begin();
	cout << "first name:" << *it << endl;

	++it;
	it->append("All");
	cout << "second name:" << *it << endl;

	while(++it != vec.end())
	{
		cout << "other name:"<< *it << endl;
	}

	for(auto ri = vec.rbegin(); ri != vec.rend(); ++ri)
	{
		cout << *ri;
		if(ri + 1 != rend(vec))
		{
			cout  << ", ";
		}
	}
	cout << endl;

	//vector<string>::const_iterator it2 = vec.begin(); //we cannot modify elements
	//*it2 += string("Modify");

	//range based for loop only forward no reverse
	for(auto&& v: vec) //&& is for more safety incase if there is an r value reference
		cout << v << endl;
	return 0;
}*/


