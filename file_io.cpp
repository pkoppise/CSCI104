#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

/*
int main()
{
    vector<int> nums;
    int x;
    ifstream myfile("data.txt");

    while(1){
      if(myfile.is_open())
      {
      	  myfile >> x;
          cout << "is:" << x << endl;

          if(myfile.fail())
          {
              break;
          }
          nums.push_back(x);

      }
      else
      {
          cout << "File is not opened" << endl;
          break;
      }
    }

    myfile.close();
}*/




